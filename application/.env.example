# -------------------------------------------------------------------------
# DNS LOOKUP APPLICATION - ENVIRONMENT CONFIGURATION
# -------------------------------------------------------------------------
# Bu dosya örnek konfigürasyonu içerir. Production ortamında bu değerler
# AWS Secrets Manager veya System Environment Variables üzerinden gelir.
# -------------------------------------------------------------------------

# --- DATABASE CONFIGURATION ---
# MongoDB sunucusunun IP adresi (Private IP)
# Örn: 10.0.11.100
MONGO_HOST=localhost

# MongoDB Portu (Varsayılan: 27017)
MONGO_PORT=27017

# Veritabanı Kullanıcı Adı
# (AWS Secrets Manager'dan otomatik çekilir, local test için buraya yazılabilir)
MONGO_USER=admin

# Veritabanı Şifresi
# UYARI: Production ortamında ASLA buraya gerçek şifre yazmayın!
MONGO_PASS=placeholder_password_for_security

# --- APP CONFIGURATION ---
# Flask uygulamasının çalışacağı port
FLASK_PORT=5889

# Debug modu (Production ortamında False olmalı)
FLASK_DEBUG=False

# AWS Bölgesi (Loglama ve Secrets Manager için gerekli)
AWS_REGION=us-east-1